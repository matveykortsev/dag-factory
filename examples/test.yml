default:
  default_args:
    owner: "infra"
    start_date: 2022-01-01
    retries: 3
    retry_delay_sec: 300
  schedule_interval: "@daily"
  max_active_runs: 1
  catchup: False
  depends_on_past: False

user_subscriptions_bikafka_test:
  description: "Reload user_subscriptions from bikafka_wide for previous day"
  tags: ['bikafka_wide', 'user_subscriptions', 'prequel']
  schedule_interval: "0 0 * * *"
  tasks:
    test_task:
      operator: airflow.operators.bash_operator.BashOperator
      bash_command: "echo 1"